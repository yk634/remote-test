shared:
  image: node:18
  steps:
    - echo: echo "test"
  annotations:
    screwdriver.cd/virtualJob: true

jobs:
  hub:
    requires: [ ~commit ]
  target:
    requires: [ sd@22674:downstream-a, sd@22674:downstream-b ]
